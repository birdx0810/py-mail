import smtplib

def send_mail(host, email, password, content):
    """

    Args:
        host (str): the mail server
        email (str): the email account
        passwprd (str): the password for logging in to email

    Returns:
        bool: indicates if email was successfully sent
    """
    return
